<h1><%= t('.events') %></h1>

<div class="my-4">
  <% if policy(Event).create? %>
    <%= link_to t('.create'), new_event_path, class: 'btn btn-outline-success' %>
  <% end %>
</div>

<div class="row mb-2">
  <%= render partial: 'events/event', collection: @events %>
</div>
